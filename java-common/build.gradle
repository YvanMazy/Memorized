plugins {
    id 'java-library'
}

dependencies {
    // Logging
    api 'org.slf4j:slf4j-api:2.0.9'

    // Annotations
    api 'org.jetbrains:annotations:24.0.1'

    // Collections
    api 'it.unimi.dsi:fastutil:8.5.12'
}